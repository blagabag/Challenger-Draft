/**
 * /draft is no longer needed since everything is button-driven.
 * Keeping this file as a no-op so deploy-commands.js doesn't break.
 * The draft sub-system lives entirely in interactionCreate.js button handlers.
 */

// This file intentionally exports nothing — all draft interaction
// is handled via buttons in events/interactionCreate.js
module.exports = {};
